<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/iconomiroti"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconomiroti"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiRotiMobile"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC8gfZthW14xhNGprWrJ_mUHsTUh2MNwg8" />

        <!-- 🔹 Pantalla principal de Login -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 MainActivity -->
        <activity android:name=".MainActivity" />

        <!-- 🔹 RegisterActivity -->
        <activity android:name=".ui.login.RegisterActivity" />

        <!-- 🔹 ResetPasswordActivity -->
        <activity android:name=".ui.login.ResetPasswordActivity" />

    </application>

</manifest>
